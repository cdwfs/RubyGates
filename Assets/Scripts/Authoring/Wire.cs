using Unity.Entities;
using UnityEngine;

// This currently exists so that GateInputs can refer to a Wire prefab
[DisallowMultipleComponent]
[RequiresEntityConversion]
public class Wire : MonoBehaviour
{
}
